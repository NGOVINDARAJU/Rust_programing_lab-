fn main() {
    let x = 5;
    println!("x in outer scope: {}", x);

    {
        let x = x + 5;
        println!("x in inner scope (shadowed): {}", x);
    }

    println!("x after inner scope: {}", x);
}

